# 提交记录分析工具

制作一个基于 Python 的提交分析工具，用于分析某个时间段内的提交记录，重点处理 Excel 等二进制文件的变更。

## 1. 目标与价值

游戏策划-程序侧开发的痛点是缺乏有效的数据修改通知和跟踪机制，尤其在大量 Excel 文件协作时更明显。本工具用于自动化识别提交中的数据变化，解释变化影响，并输出可执行的跟进清单。

与传统 Changelog 自动化工具相比，本工具增加 AI 语义分析能力，目标是提高风险识别准确度和处理效率。

## 2. AI 职责边界

AI 只负责语义分析和建议生成，不负责原始数据篡改。

- 关键变更：影响数据结构、主键或 ID、字段语义、掉落或数值平衡、跨表引用关系的修改。
- 潜在风险：可能导致数据丢失、引用失效、配置不兼容、线上行为异常的修改。
- 跟进事项：需要人工确认、回归测试、补充文档、通知相关负责人的事项。

## 3. 执行优先级

先完成主流程，再处理扩展能力。

1. 通过 Python 将 Excel 转换为文本格式（CSV）。
2. 分析 CSV 的变更记录并输出分析结果（可清理中间 CSV，保留分析结果）。
3. 将分析结果整理为 TODO 列表，便于后续处理和跟踪。
4. 在主流程稳定后接入 CI/CD、自动提交和多运行模式。

## 4. 总体架构（分层）

1. 入口编排层
	- 解析参数、时间范围、分支、模式。
	- 编排任务顺序、失败重试、日志输出。
2. 提交采集层
	- 获取提交列表与文件变更清单。
	- 按文件类型分流（Excel、CSV、JSON、其他文本）。
3. 制品还原与转换层
	- 还原变更前后版本。
	- Excel 转 CSV，保证编码与字段顺序稳定。
4. 结构化 Diff 层
	- 从文本差异转为结构化差异（表、行、列、单元格）。
5. 规则分析层
	- 根据可解释规则先行识别高风险变更。
6. AI 语义分析层
	- 对结构化差异做语义归因、影响范围分析、建议生成。
7. 聚合与评分层
	- 合并规则与 AI 结果，去重、冲突裁决、风险分级。
8. 输出与集成层
	- 输出报告、TODO、JSON；可选自动提交或 CI 注释。

## 5. 关键数据模型

1. CommitEvent
	- commit_id, author, time, message, changed_files。
2. FileChange
	- file_path, file_type, before_ref, after_ref, status。
3. StructuredDiff
	- table, row_key, column, before_value, after_value, change_type。
4. RiskItem
	- risk_type, severity, confidence, evidence, impact。
5. TodoItem
	- title, priority, owner_hint, due_hint, action, verify_steps。
6. AnalysisReport
	- summary, key_changes, risks, todos, metrics。

## 6. 输入与输出契约

输入：

- 时间范围或提交范围（例如 since-until、base-head）。
- 目标分支或仓库路径。
- 运行模式（agent 或 api）。
- 规则配置与风险阈值配置。

输出：

- Markdown 分析报告（给人看）。
- JSON 结果文件（给程序消费）。
- TODO 列表（可追踪、可分派）。

## 7. TODO 产物规范

每条 TODO 至少包含以下字段：

1. 标题：一句话描述问题。
2. 优先级：P0/P1/P2。
3. 触发证据：对应提交、文件、关键字段。
4. 建议动作：需要执行的修复或确认动作。
5. 验证步骤：如何确认问题已关闭。
6. 责任提示：建议通知的角色或模块负责人。

## 8. 运行模式

### 8.1 Agent 代理模式

通过 Prompt 文件和 Python 脚本，调用 Copilot 或其他 Agent 分析提交变更。

适用场景：

- 需要人机协作解释复杂变更。
- 需要临时补充上下文进行诊断。

### 8.2 API 调用模式

通过 .env 定义的 API Key 调用远程 AI 服务。

适用场景：

- CI/CD 自动执行。
- 大批量、标准化分析任务。

无论哪种模式，最终都必须输出标准 TODO 列表。

## 9. CI/CD 接入要求

在主流程稳定后，支持 GitHub Workflow 调用：

1. 触发条件：push、pull_request 或特定标签。
2. 执行内容：变更分析、风险汇总、TODO 生成。
3. 产物策略：上传报告与 JSON，必要时回写仓库。
4. 阻断策略：高风险阈值可配置为阻断或仅告警。

## 10. 非功能性要求

1. 可重复：同一输入应产出稳定结果。
2. 可解释：每条风险和 TODO 必须附证据。
3. 可扩展：新增文件类型不影响主干流程。
4. 可降级：AI 调用失败时仍可输出规则分析结果。
5. 可观察：提供基础日志、耗时统计、错误摘要。

## 11. 里程碑建议

1. MVP
	- 完成提交采集、Excel 转 CSV、结构化 diff、规则分析、TODO 输出。
2. V1
	- 接入 AI 语义分析与风险评分。
3. V1.5
	- 接入 GitHub Workflow 与自动回写机制。
4. V2
	- 增加跨表影响链分析和模型提示优化。

## 12. 一句话原则

先稳定结构化数据链路，再增强 AI 分析能力；先保证输出可追踪，再追求分析智能化。
